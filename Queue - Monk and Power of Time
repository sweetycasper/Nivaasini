#include <stdio.h>

int main()
{
	int n = 3;
	int f;
	int a[n] = {3,,2,1};
	int b[n] = {1,3,2};
	int c = 0;
	if(c< n)
	{
	    int d;
	    d = b[c];
	    if(a[c] == d)
	    {
	        break;
	    }
	    int e;
	    e =c;
	    while(a[e] != d)
	    {
	        e++;
	    }
	    int temp;
	    temp = a[e];
	    a[e] = a[c];
	    c++;
	    f++;
	}
	int front = -1;
	int rear = n-1;
	if(front == rear)
	{
	    break;
	}
	else
	{
	    front = front +1;
	    queue();
	}
	queue()
	{
	    return a[front];
	    f++;
	}
	printf("&d",f);
	}
